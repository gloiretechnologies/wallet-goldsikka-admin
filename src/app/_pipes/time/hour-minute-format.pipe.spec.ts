import { HourMinuteFormatPipe } from './hour-minute-format.pipe';

describe('HourMinuteFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HourMinuteFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
